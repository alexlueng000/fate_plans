# 微信支付退款功能实施说明

## 1. 文档信息

- 项目：Fate 八字命理应用
- 实施范围：微信支付整单退款闭环
- 涉及项目：`fate_backend`、`fate_frontend`
- 实施日期：2026-07-20 至 2026-07-21
- 当前完成阶段：第 1–6 步

## 2. 实施目标

在现有微信 Native 支付和会员权益发放能力上，补充安全、可追溯、可重试的退款闭环，包括：

1. 建立退款单和权益流水数据模型。
2. 让每笔新支付订单发放的额度和会员期限可追溯。
3. 提供管理员整单退款接口。
4. 接入微信支付退款申请、退款查询及退款结果回调。
5. 退款成功后幂等回收对应订单权益。
6. 提供管理后台订单与退款操作页面。

本阶段仅支持管理员发起的微信支付整单全额退款，不支持用户自助退款和部分退款。

## 3. 整体退款流程

```text
管理员选择已支付订单
        ↓
创建本地退款单并生成固定 out_refund_no
        ↓
订单状态变为 REFUNDING
        ↓
调用微信退款申请 API
        ↓
微信返回 PROCESSING / SUCCESS / CLOSED / ABNORMAL
        ↓
退款回调或管理员主动查询同步状态
        ↓
退款状态确认 SUCCESS
        ↓
写入额度反向流水
        ↓
回收尚未使用额度
        ↓
撤销该订单贡献的会员期限
        ↓
订单状态变为 REFUNDED
```

微信退款申请返回成功只代表微信已受理。最终状态以退款结果回调或主动查询结果为准。

## 4. 数据库改造

迁移文件：

```text
fate_backend/migrations/add_refund_ledger_tables.sql
```

### 4.1 refunds 退款单

用于保存本地退款申请、微信退款标识、退款状态和错误信息。

主要字段：

| 字段 | 说明 |
|---|---|
| `order_id` | 原支付订单 |
| `user_id` | 订单用户 |
| `out_refund_no` | 商户退款单号，全局唯一 |
| `wechat_refund_id` | 微信退款单号，全局唯一 |
| `refund_cents` | 退款金额，单位为分 |
| `total_cents` | 原订单金额，单位为分 |
| `currency` | 币种，目前为 CNY |
| `reason` | 退款原因 |
| `status` | 本地退款状态 |
| `requested_by` | 发起退款的管理员 |
| `success_at` | 退款成功时间 |
| `raw_request` | 调用微信退款接口的请求体 |
| `raw_response` | 微信接口或回调原始响应 |
| `failure_code` | 失败或人工处理代码 |
| `failure_message` | 失败详情 |

关键约束：

- `out_refund_no` 唯一。
- `wechat_refund_id` 唯一。
- 退款金额必须大于零。
- 退款金额不能超过原订单金额。
- 订单、用户等审计外键使用 `RESTRICT`，避免删除业务记录后丢失退款审计数据。

### 4.2 quota_ledger 额度流水

用于记录每一次订单额度发放和退款撤销。

主要字段：

| 字段 | 说明 |
|---|---|
| `user_id` | 用户 |
| `quota_type` | 额度类型，如 `chat`、`liuyao_chat` |
| `delta` | 额度变化，发放为正数，退款撤销为负数 |
| `event_type` | `PURCHASE_GRANT` 或 `REFUND_REVERSAL` |
| `order_id` | 对应支付订单 |
| `refund_id` | 对应退款单 |
| `idempotency_key` | 幂等键，全局唯一 |
| `note` | 实际回收和未回收额度等审计信息 |

购买发放幂等键：

```text
order:{order_id}:quota:{quota_type}:grant
```

退款撤销幂等键：

```text
refund:{refund_id}:quota:{quota_type}:reversal
```

### 4.3 membership_grants 会员期限流水

用于记录每笔会员订单单独贡献的会员期限。

主要字段：

| 字段 | 说明 |
|---|---|
| `user_id` | 用户 |
| `order_id` | 对应支付订单，每个订单唯一 |
| `product_id` | 会员商品 |
| `starts_at` | 该订单贡献期限的开始时间 |
| `ends_at` | 该订单贡献期限的结束时间 |
| `status` | `ACTIVE`、`REVOKED` 等 |
| `refund_id` | 导致撤销的退款单 |
| `revoked_at` | 撤销时间 |

关键约束：

- 每笔订单最多生成一条会员期限流水。
- 结束时间必须晚于开始时间。
- 退款只撤销对应订单贡献的期限，不会直接关闭其他订单购买的会员期限。

## 5. 新订单权益追踪

支付成功调用 `apply_paid_product` 时，现在会在同一事务中完成：

1. 将支付订单标记为已支付。
2. 发放用户额度。
3. 写入 `PURCHASE_GRANT` 额度流水。
4. 月卡订单写入独立 `membership_grants` 记录。
5. 更新用户会员汇总期限。

同一个支付订单被重复处理时：

- 额度幂等键阻止重复发放。
- `membership_grants.order_id` 唯一约束阻止重复延长会员期限。
- 订单、额度、会员和流水由外层事务统一提交，降低部分成功风险。
- 更新聚合额度时使用数据库行锁，降低并发购买造成的额度覆盖风险。

历史订单不会自动补充权益流水。部署本功能之前的订单可能需要人工核实和处理。

## 6. 订单及退款状态

### 6.1 订单状态

```text
CREATED
PAID
REFUNDING
REFUNDED
PARTIALLY_REFUNDED
CANCELED
```

当前整单退款主要状态流：

```text
PAID → REFUNDING → REFUNDED
```

### 6.2 退款状态

```text
CREATED
PROCESSING
SUCCESS
CLOSED
ABNORMAL
FAILED
```

说明：

- `CREATED`：本地退款单已创建，尚未成功提交微信。
- `PROCESSING`：微信已受理并处理中。
- `SUCCESS`：微信退款成功。
- `CLOSED`：退款已关闭。
- `ABNORMAL`：退款异常，需要人工处理。
- `FAILED`：本次调用微信退款接口失败，可复用相同商户退款单号重试。

退款状态同步具有防倒退能力。例如已经收到 `SUCCESS` 回调后，延迟到达的 `PROCESSING` 查询结果不会覆盖成功状态。

## 7. 管理员后端接口

所有管理员接口都需要管理员 Bearer Token。

### 7.1 管理员订单列表

```http
GET /api/admin/orders
```

查询参数：

| 参数 | 说明 |
|---|---|
| `page` | 页码，从 1 开始 |
| `page_size` | 每页数量，1–100 |
| `order_status` | 订单状态筛选 |
| `refund_status` | 退款状态筛选 |
| `search` | 搜索订单号、用户、商品名称或商品编码 |

返回内容包括：

- 用户信息。
- 商品信息。
- 订单金额和状态。
- 最新支付渠道及微信交易号。
- 最新退款信息。
- 权益追踪状态：`READY`、`MISSING`、`NOT_REQUIRED`。

### 7.2 发起整单退款

```http
POST /api/admin/orders/{order_id}/refund
Content-Type: application/json

{
  "reason": "用户申请退款"
}
```

接口行为：

1. 校验管理员权限。
2. 对订单加数据库行锁。
3. 校验订单状态必须为 `PAID`。
4. 校验实付金额大于零、币种为人民币。
5. 校验存在成功微信支付记录及 `transaction_id`。
6. 退款金额强制从订单数据库读取，不接受前端传入金额。
7. 创建固定商户退款单号并先提交本地事务。
8. 调用微信退款申请 API。
9. 验证微信 API 响应签名。
10. 保存微信退款状态及原始响应。

同一订单重复调用时会复用已有退款单，不会重复创建退款。

### 7.3 主动同步退款状态

```http
POST /api/admin/refunds/{refund_id}/sync
```

调用微信接口：

```text
GET /v3/refund/domestic/refunds/{out_refund_no}
```

同步微信最新退款状态，主要用于回调延迟、回调丢失或人工排查。

### 7.4 微信退款回调

```http
POST /api/webhooks/wechatpay/refund
```

回调处理包括：

- 验证微信支付签名。
- 使用 APIv3 Key 解密 AES-256-GCM 资源数据。
- 校验退款事件类型。
- 校验商户退款单号。
- 校验原商户订单号。
- 校验退款金额、订单金额和币种。
- 对退款单加数据库行锁。
- 幂等同步微信退款状态。
- 写入 webhook 审计日志。
- 退款成功后执行权益回收。

支持的事件：

```text
REFUND.SUCCESS
REFUND.CLOSED
REFUND.ABNORMAL
```

## 8. 微信退款 API

### 8.1 申请退款

```text
POST https://api.mch.weixin.qq.com/v3/refund/domestic/refunds
```

核心请求：

```json
{
  "transaction_id": "微信支付订单号",
  "out_refund_no": "商户退款单号",
  "reason": "用户申请退款",
  "notify_url": "https://api.fateinsight.site/api/webhooks/wechatpay/refund",
  "amount": {
    "refund": 3990,
    "total": 3990,
    "currency": "CNY"
  }
}
```

### 8.2 查询退款

```text
GET https://api.mch.weixin.qq.com/v3/refund/domestic/refunds/{out_refund_no}
```

申请和查询接口的成功响应均使用配置的微信平台公钥验证响应签名。

## 9. 退款成功后的权益回收

### 9.1 额度回收规则

退款成功后，系统根据原订单的 `PURCHASE_GRANT` 流水确定原始发放额度，并写入 `REFUND_REVERSAL` 反向流水。

聚合额度只回收当前尚未使用的部分，不会让：

```text
total_quota < used_quota
```

示例：

- 原订单发放：100 次。
- 当前可安全回收：60 次。
- 已消费、无法回收：40 次。

反向流水：

```text
delta = -100
note = granted=100;reclaimed=60;unrecovered=40
```

含义：

- 业务账本完整撤销该订单发放的 100 次权益。
- 用户聚合额度实际只减少 60 次，避免产生负额度。
- 已消费的 40 次保留为审计信息。

### 9.2 会员期限回收规则

退款成功后：

1. 找到对应订单的 `membership_grants`。
2. 将该条记录标记为 `REVOKED`。
3. 写入 `refund_id` 和 `revoked_at`。
4. 从用户会员汇总到期时间中扣除该订单贡献的期限。
5. 如果扣除后已经到期，将会员状态设为 `cancelled`。
6. 不影响其他订单贡献的会员期限。

### 9.3 历史订单保护

如果微信退款成功，但历史订单缺少额度或会员追踪流水：

- 微信退款状态仍保存为 `SUCCESS`。
- 订单暂时保持 `REFUNDING`。
- 系统不会盲目扣除用户其他订单权益。
- 退款单记录：

```text
failure_code = ENTITLEMENT_TRACE_MISSING
```

- 管理后台显示“历史订单缺失”警告，需要管理员人工核实。

## 10. 管理后台页面

页面地址：

```text
/admin/orders
```

后台首页新增“订单与退款”入口。

页面功能：

- 分页查看订单。
- 搜索订单号、用户及商品。
- 按订单状态筛选。
- 按退款状态筛选。
- 查看支付渠道、交易号、金额和用户信息。
- 查看退款单号、退款状态及失败原因。
- 查看权益流水是否完整。
- 对已支付订单发起整单退款。
- 填写退款原因并进行二次确认。
- 主动同步微信退款状态。
- 对历史订单权益流水缺失进行醒目提示。

前端主要文件：

```text
fate_frontend/app/(auth)/admin/orders/page.tsx
fate_frontend/app/(auth)/admin/page.tsx
fate_frontend/app/lib/api.ts
```

## 11. 后端主要文件

```text
fate_backend/app/models/refund.py
fate_backend/app/models/quota_ledger.py
fate_backend/app/models/membership_grant.py
fate_backend/app/models/__init__.py
fate_backend/app/services/refunds.py
fate_backend/app/services/membership_service.py
fate_backend/app/services/quota.py
fate_backend/app/services/payments.py
fate_backend/app/routers/admin_refunds.py
fate_backend/app/routers/webhooks.py
fate_backend/app/schemas/__init__.py
fate_backend/app/config.py
fate_backend/main.py
fate_backend/migrations/add_refund_ledger_tables.sql
```

测试文件：

```text
fate_backend/app/test/test_paid_entitlements.py
fate_backend/app/test/test_refunds.py
```

## 12. 环境变量

生产环境需要确认以下配置：

```env
WECHAT_PAY_MODE=prod
WECHAT_PAY_APPID=
WECHAT_PAY_MCHID=
WECHAT_PAY_MERCHANT_SERIAL_NO=
WECHAT_PAY_PRIVATE_KEY_PATH=
WECHAT_PAY_PRIVATE_KEY_PEM=
WECHAT_PAY_NOTIFY_URL=https://api.fateinsight.site/api/webhooks/wechatpay
WECHAT_PAY_REFUND_NOTIFY_URL=https://api.fateinsight.site/api/webhooks/wechatpay/refund
WECHAT_API_V3_KEY=
WECHAT_PLATFORM_PUBLIC_KEY_PATH=
WECHAT_PLATFORM_PUBLIC_KEY_PEM=
```

如果未单独配置 `WECHAT_PAY_REFUND_NOTIFY_URL`，系统会尝试在 `WECHAT_PAY_NOTIFY_URL` 后追加 `/refund`。

开发模式不会真实退款，会生成模拟微信退款单号并将退款状态设为 `PROCESSING`，用于本地联调。

## 13. 部署顺序

必须严格按以下顺序部署：

1. 备份生产数据库。
2. 执行数据库迁移：

```bash
mysql -u <user> -p <database> < fate_backend/migrations/add_refund_ledger_tables.sql
```

3. 验证新表：

```sql
SHOW TABLES LIKE 'refunds';
SHOW TABLES LIKE 'quota_ledger';
SHOW TABLES LIKE 'membership_grants';
```

4. 更新生产环境退款回调配置。
5. 部署 `fate_backend`。
6. 在 `fate_frontend` 安装新增依赖：

```bash
npm ci
```

7. 部署 `fate_frontend`。
8. 在微信商户配置及网络策略中确认退款回调地址可从公网访问。
9. 使用小额真实订单完成一次退款验收。

不能先部署后端代码再执行数据库迁移，否则新的支付成功回调会因为缺少权益流水表而失败。

## 14. 验证结果

本次实施已完成以下验证：

- 后端 Python 语法检查通过。
- `git diff --check` 通过。
- 退款与权益相关 12 个单元测试全部通过。
- 覆盖重复支付发放、重复退款回调、额度部分已使用、月卡续期撤销等场景。
- 管理后台新增和修改文件 ESLint 检查通过。
- TypeScript 全量检查当前仅受本地未安装 `qrcode` 依赖影响；执行 `npm install` 或 `npm ci` 后可解决。

## 15. 当前限制及后续建议

当前限制：

- 仅支持管理员发起退款。
- 仅支持微信支付整单全额退款。
- 不支持部分退款。
- 历史订单没有自动回填权益流水。
- 尚未提供定时自动查询长期处于 `PROCESSING` 的退款。
- 尚未提供每日微信退款对账任务。
- 尚未提供用户自助退款申请及审核工单。

建议后续实施：

1. 增加退款状态补偿任务，定期查询长时间处于 `PROCESSING` 的退款。
2. 增加每日退款对账和异常告警。
3. 为历史订单提供人工回填或人工退款处理工具。
4. 增加管理员退款审计报表。
5. 根据服务协议和业务政策决定是否开放用户自助退款。
6. 业务稳定后再评估部分退款。

