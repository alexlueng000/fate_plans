# Fate Web UI 评估与优化建议

> 评估日期：2026-07-29  
> 评估范围：Web 前端，不包含微信小程序及其他移动端  
> 总体判断：当前 UI 已形成米白背景、朱红主色、衬线标题与克制留白的东方文化气质；下一阶段应重点提升设计一致性、信息层级、关键操作效率和移动 Web 体验。

## 一、执行摘要

当前 UI 的主要问题不是视觉方向错误，而是不同页面的设计语言和信息结构还没有完全统一。部分页面偏重氛围和展示，关键任务的“下一步做什么”与结果页的“我得到了什么”还可以更突出。

建议保留现有温润、克制的东方文化风格，同时减少装饰性信息和卡片堆叠，使视觉层级更多服务于：

1. 降低首次理解和选择成本。
2. 提高排盘、任务和付费流程的完成效率。
3. 让复杂命理信息更容易阅读和理解。
4. 建立跨页面一致的设计语言。
5. 完善加载、失败、空状态和移动端体验。

## 二、UI 优化建议

### 1. 建立统一的页面模板

**现状问题**

首页、定价页、排盘页、聊天页、会员页和专题页在标题尺寸、顶部间距、内容宽度、卡片形态和操作区位置上存在一定差异。

**改进建议**

定义四类标准页面模板：

- 营销页：首页、示例报告、定价页。
- 工具页：排盘、六爻、心镜灯。
- 任务页：职业、感情及后续专题任务。
- 账户页：会员、历史记录、个人中心。

每类模板统一：

- 页面最大宽度。
- 顶部和底部留白。
- 标题及说明文字层级。
- 卡片间距和分区方式。
- 页面操作区位置。
- 桌面端与移动端适配规则。

### 2. 收敛字体体系

**现状问题**

衬线、无衬线、等宽字体以及不同字号组合强化了文化感，但过多组合也容易形成视觉噪声。

**改进建议**

- 页面大标题使用衬线字体。
- 正文、表单和按钮使用无衬线字体。
- 数字、日期和额度使用等宽字体。
- 避免在小字号、长段落和操作按钮中使用衬线字体。
- 中文主要正文建议使用 15～16px，最小正文不低于 14px。
- 减少大量 10px、11px、12px 的英文大写装饰标签。

### 3. 简化颜色层级

**现状问题**

页面存在主文本、正文、次级文本、弱文本和提示文本等多个相近灰度层级，实际使用中容易显得零碎。

**改进建议**

将文字颜色收敛为四档：

1. 主标题。
2. 正文。
3. 辅助说明。
4. 禁用状态。

朱红色主要用于：

- 核心 CTA。
- 当前选中状态。
- 重要数据。
- 风险或错误提示。

品牌朱红与错误红应使用不同色值，避免用户把品牌元素误认为异常状态。

### 4. 统一圆角、边框和阴影

**现状问题**

部分页面偏直角编辑风格，部分页面使用较大圆角和明显卡片感，容易产生来自不同产品的观感。

**改进建议**

统一为三档形态：

- 小圆角：输入框、标签和轻量按钮。
- 中圆角：普通卡片和弹窗内容。
- 大圆角：只用于少数核心展示模块。

减少阴影使用，优先通过背景层级、边框与留白划分内容。东方文化风格更适合克制层次，不宜大量使用悬浮卡片。

### 5. 首页首屏减少文字并强化核心动作

**现状问题**

首页首屏同时包含产品类型、主标题、长描述、两个 CTA、信任说明和三功能列表，用户需要在短时间内处理较多信息。

**改进建议**

首屏只需要回答三个问题：

- 这是什么？
- 能给用户什么？
- 下一步做什么？

推荐主文案：

> 从传统文化中，看见理解自己的新视角。  
> 输入出生信息，获得一份个人特质分析。

首屏只保留一个主按钮：“免费生成个人分析”。其他功能在第二屏继续介绍。

### 6. 三大功能采用主次分明的视觉布局

**现状问题**

八字、心镜灯和六爻在首页被平行展示，但三者在用户旅程中的作用并不相同。

**改进建议**

- 八字作为首次获客入口，使用大型主卡片。
- 六爻作为具体问题工具，使用次级卡片。
- 心镜灯作为日常留存工具，使用次级卡片。
- 课程放在更靠后的内容区。

通过视觉权重体现用户路径，降低首次选择成本。

### 7. 谨慎使用用户语录

**现状问题**

首页采用“姓名、年龄、职业、使用感受”的用户语录形式。如果内容并非真实且获得授权的评价，可能降低产品可信度。

**改进建议**

- 真实评价标注“来自用户反馈”，并隐藏敏感信息。
- 非真实评价改为“典型使用场景”，不要包装成真实用户。
- 可以用问题卡替代，例如“我适合换工作吗？”“这段关系应该如何梳理？”

### 8. 将出生信息表单改为分步填写

**现状问题**

性别、历法、日期、时间和地点一次全部出现，桌面端可以接受，但移动 Web 容易形成较长表单。

**改进建议**

拆分为三步：

1. 出生日期和时间。
2. 出生地点与真太阳时说明。
3. 信息确认并开始分析。

顶部显示轻量进度，保持已填写内容，并允许返回修改。

### 9. 提升出生地点输入体验

**现状问题**

直接输入城市可能遇到同名地区、区县层级或海外地点歧义。

**改进建议**

- 提供城市联想搜索。
- 展示省、市、区完整结果。
- 允许用户主动授权使用当前位置。
- 选择后显示“已用于真太阳时修正”。
- 地点不确定时提示可能产生的计算误差。

### 10. 增加出生时间不确定状态

**现状问题**

很多用户并不知道精确出生分钟。强制选择具体时间会制造错误的精确感。

**改进建议**

提供以下选项：

- 准确时间。
- 大约时间。
- 只知道时辰。
- 不清楚出生时间。

选择不确定状态后，说明哪些结果仍可查看、哪些结论可能受到影响。

### 11. 表单错误应就地反馈

**现状问题**

如果错误仅通过顶部提示或 Toast 显示，用户可能不知道需要修改哪个字段。

**改进建议**

- 对应输入框显示错误边框。
- 错误说明紧邻字段。
- 自动聚焦第一个错误项。
- 修正后立即清除错误。
- 保留其他已经填写的内容。

同时区分输入错误、网络错误、服务端计算失败和额度不足，不要统一显示“请求失败”。

### 12. 结果页采用“摘要优先、专业信息后置”

**现状问题**

专业命盘信息较多，普通用户首先关心的是这些数据与自己有什么关系，而不是完整术语和计算结果。

**改进建议**

结果页分为四层：

1. 一句话总结。
2. 三个关键洞察。
3. 当前关注主题。
4. 完整命盘与专业解释。

日主、五行、十神、大运等专业术语提供点击展开的轻量解释，避免常驻大段说明。

### 13. 为长报告建立固定导航

**现状问题**

长报告容易让用户失去当前位置，也不利于快速返回感兴趣章节。

**改进建议**

桌面端使用侧边目录：

- 总览。
- 性格特质。
- 优势与挑战。
- 职业。
- 感情。
- 趋势。
- 行动建议。

移动端使用横向吸顶标签或底部章节按钮，并补充当前阅读进度、返回顶部、收藏章节和继续追问。

### 14. 减少卡片套卡片

**现状问题**

如果命盘、五行、解读段落和提示信息全部使用独立卡片，页面会出现过多边框与容器，内容关系反而不清晰。

**改进建议**

- 主要章节通过留白和标题分区。
- 只有可操作、可展开或需要强调的内容才使用卡片。
- 同一章节内部优先使用列表、分隔线和背景层级。
- 避免在大卡片内部继续嵌套多层小卡片。

### 15. 优化五行可视化，避免伪精确

**现状问题**

柱状图或雷达图容易让用户误以为相关数值具有非常精确的科学含义。

**改进建议**

- 使用“偏强、适中、偏弱”等区间表达。
- 图表同时配合文字结论。
- 提供简短的计算口径说明。
- 不只依赖颜色区分，兼顾色觉障碍。
- 避免使用精确到小数点的百分比。

### 16. 将推荐问题放到聊天首屏

**现状问题**

空白输入框要求用户自己组织问题，会提高首次启动成本。

**改进建议**

首次进入时展示 3～5 个动态推荐问题，例如：

- 我的优势更适合什么工作方式？
- 最近为什么容易感到犹豫？
- 未来一年应该重点关注什么？
- 帮我比较当前的两个选择。
- 请用通俗语言解释我的命盘。

推荐问题应结合当前报告动态生成，而不是所有用户完全相同。

### 17. 区分分析结论与普通对话

**现状问题**

重要结论容易淹没在长对话气泡中，用户后续难以回顾和复用。

**改进建议**

AI 回答采用稳定结构：

- 结论。
- 判断依据。
- 需要注意的条件。
- 可以采取的行动。
- 建议继续追问。

重要内容支持一键加入“我的结论”。长分析可以使用文档式排版，不必全部限制在传统聊天气泡中。

### 18. 优化流式生成状态

**现状问题**

AI 长回答生成时，如果只有文字不断出现，用户可能无法判断服务是否仍在正常工作。

**改进建议**

- 显示“正在结合命盘分析”等真实状态。
- 展示当前分析阶段，但不使用虚假百分比。
- 支持停止生成和重新生成。
- 网络中断后允许继续。
- 用户离开页面时保存生成状态。

### 19. 控制聊天和报告正文宽度

**现状问题**

桌面端长文本占据过宽区域时，阅读效率会明显下降。

**改进建议**

- 正文阅读宽度控制在约 680～760px。
- 长段落拆分为较短段落。
- 列表保持足够的行距和段间距。
- 标题与正文采用稳定的垂直节奏。
- 专业解释放入可展开区域。
- 用户短消息与 AI 长报告采用不同排版方式。

### 20. 精简顶部导航

**现状问题**

首页、排盘、六爻、心镜灯、职业、感情、课程、历史和会员等入口如果都占据一级导航，会造成拥挤和认知负担。

**改进建议**

一级导航建议收敛为：

- 探索。
- 我的分析。
- 心镜灯。
- 课程。
- 我的。

职业、感情和六爻可进入“探索”或作为上下文推荐，不必全部长期占据一级导航。

### 21. 为核心任务建立移动端固定操作区

**现状问题**

移动 Web 的长页面中，主要按钮容易滚动出视野。

**改进建议**

以下页面可采用底部固定操作栏：

- 排盘表单：“生成分析”。
- 报告页：“继续追问”。
- 六爻结果：“开始解读”。
- 定价页：“选择套餐”。
- 任务页：“保存并继续”。

固定操作栏需要适配系统安全区域，避免遮挡正文和输入框。

### 22. 完善加载、空状态和失败状态

**现状问题**

产品包含大量异步数据和 AI 生成流程，状态体验会直接影响用户对产品稳定性和品质的判断。

**改进建议**

为主要页面统一设计：

- 首次加载骨架屏。
- 无历史记录。
- 暂无课程。
- 无情绪记录。
- AI 生成失败。
- 网络断开。
- 支付状态确认中。
- 数据已删除。
- 额度已耗尽。

每个状态都要提供明确的下一步操作。

### 23. 提高小字号和浅色文字的可读性

**现状问题**

较多浅灰色、低对比度和小字号装饰文字在户外环境、高亮屏幕或视力较弱用户中可能难以阅读。

**改进建议**

- 保证正文与背景具有足够对比度。
- 关键说明不使用过浅颜色。
- 按钮文字不低于 14px。
- 可点击区域至少约 44×44px。
- 不使用颜色作为唯一状态识别方式。

### 24. 补齐键盘操作与焦点状态

**改进建议**

- Tab 可以顺序访问所有操作控件。
- 当前焦点具有清晰可见的样式。
- 弹窗打开后将焦点移动到弹窗内部。
- 弹窗关闭后将焦点返回原按钮。
- Esc 可以关闭非关键弹窗。
- Enter 不会误提交长文本。
- 所有图标按钮都有文字标签或辅助说明。

### 25. 统一动效原则

**现状问题**

动效如果过多用于装饰，可能削弱当前产品克制、稳重的气质。

**建议保留**

- 页面进入的轻微淡入。
- 卡片展开和收起。
- 提交后的状态反馈。
- 流式回答状态。
- 页面内定位滚动。

**建议减少**

- 多个元素依次出现的长动画。
- 大幅度位移。
- 持续循环的装饰动画。
- 每张卡片不同的悬浮效果。

同时支持操作系统的“减少动态效果”设置。

## 三、实施优先级

### P0：直接影响转化与任务完成率

1. 首页首屏做减法并统一主 CTA。
2. 排盘表单分步化。
3. 结果页改为摘要优先。
4. 聊天推荐问题前置。
5. 移动端增加固定操作区。
6. 优化表单错误和异步状态。

**阶段目标：** 降低首次使用门槛，提升排盘完成率、首次提问率和关键任务完成率。

### P1：提升一致性和整体品质

1. 建立标准页面模板。
2. 收敛字体、颜色、圆角、边框和卡片规范。
3. 为长报告增加固定导航。
4. 将 AI 长回答逐步文档化。
5. 统一加载、空状态和失败状态。
6. 精简顶部导航。

**阶段目标：** 建立稳定的视觉语言，提高长内容的阅读效率和跨页面一致性。

### P2：建立差异化体验

1. 优化五行信息可视化。
2. 支持结论收藏和报告资产化。
3. 建立隐私安全的分享卡片。
4. 完善键盘、无障碍和动效细节。

**阶段目标：** 提升产品专业感、内容复用价值和自然传播能力。

## 四、建议建立的 UI 验收指标

| 体验环节 | 建议指标 |
| --- | --- |
| 首页 | 首屏主 CTA 点击率、首屏退出率 |
| 排盘表单 | 开始填写率、字段错误率、表单完成率、平均完成时间 |
| 结果页 | 核心摘要查看率、章节展开率、继续追问率 |
| 聊天页 | 推荐问题点击率、首次提问率、回答中断率 |
| 定价与支付 | 套餐点击率、订单创建率、支付状态中断率 |
| 移动 Web | 固定操作栏点击率、输入框遮挡或误触反馈量 |
| 可用性 | 加载失败率、错误恢复率、无障碍问题数量 |

## 五、设计原则

后续 UI 设计与验收建议遵循以下原则：

1. 每个页面只突出一个主要动作。
2. 先展示用户能够理解的结论，再展示专业信息。
3. 使用留白和排版建立层级，减少不必要的卡片与阴影。
4. 品牌气质不能以牺牲可读性和操作效率为代价。
5. 移动 Web 应作为核心使用环境设计，而不是桌面页面的简单缩小。
6. 所有异步流程都必须具备加载、成功、失败和恢复状态。
7. 不使用虚假精确的数据表现或生成进度。
8. 涉及个人信息和分享时，默认采用最小披露原则。

## 六、最终结论

当前 UI 的品牌方向是成立的，不需要推倒重做。更合适的策略是保留温润、克制的东方文化气质，同时系统性地统一页面模板、视觉变量、信息层级和交互状态。

最终优化方向可以概括为：

> **减少装饰性信息和卡片堆叠，让视觉层级优先回答“下一步做什么”和“我得到了什么”。**
