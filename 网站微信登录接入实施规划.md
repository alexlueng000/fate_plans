# 网站微信登录接入实施规划

> 文档日期：2026-08-25  
> 适用范围：`fate_frontend/` Next.js 网站与 `fate_backend/` FastAPI 后端  
> 不在范围：微信小程序、`wx.login()` 以及现有小程序登录链路

## 一、背景与目标

网站当前支持邮箱密码登录、手机验证码登录，登录成功后由后端签发 JWT，前端将 `auth_token` 保存在 `localStorage`，并将用户摘要保存在 `sessionStorage.me`。

本项目的目标是在不改变现有 JWT 使用方式的前提下，增加网站微信登录，同时覆盖：

1. PC 或普通浏览器中的微信扫码登录。
2. 微信内置浏览器中的公众号网页授权登录。
3. 微信登录与邮箱、手机号登录共用同一个站内用户、订单、权益和命盘档案。
4. 后续可以安全增加微信绑定、解绑和账号合并。

## 二、已确认的技术决策

### 2.1 按访问环境选择微信授权渠道

| 访问环境 | 登录方式 | 微信侧能力 | 渠道标识 |
|---|---|---|---|
| PC/普通浏览器 | 微信扫码登录 | 微信开放平台网站应用 | `wechat_open` |
| 微信内置浏览器 | 网页 OAuth 授权 | 公众号网页授权 | `wechat_official` |

前端可以根据 User-Agent 选择交互方式，但 User-Agent 不作为安全校验依据。后端必须通过渠道、AppID、回调地址和 `state` 确认授权来源。

### 2.2 不复用小程序登录

现有 `/api/auth/mp/login`、`jscode2session`、`users.openid` 和 `js_code=dev` 属于遗留小程序链路。新网站功能不调用、不扩展这条链路。

小程序代码和接口的删除应作为独立清理任务，不与本次微信登录上线捆绑，避免影响存量数据或未知调用方。

### 2.3 不在 URL 中传递 JWT

微信授权回调后，后端只向前端传递短效、一次性的登录票据 `ticket`。前端再通过 POST 请求将 `ticket` 兑换为现有 JWT。

这样可以避免 JWT 出现在：

- 浏览器历史。
- CDN/反向代理访问日志。
- 第三方统计脚本。
- `Referer` 请求头。
- 用户截图或复制的链接。

## 三、微信平台前置准备

### 3.1 普通浏览器扫码登录

需要准备：

1. 微信开放平台账号和已完成的开发者资质认证。
2. 已创建并审核通过的“网站应用”。
3. 网站应用 AppID 和 AppSecret。
4. 已配置授权回调域名。
5. 网站域名、ICP 备案和应用信息符合微信审核要求。

### 3.2 微信内置浏览器登录

需要准备：

1. 拥有网页授权能力的已认证公众号。
2. 公众号 AppID 和 AppSecret。
3. 在公众号后台配置网页授权域名。
4. 将公众号与网站应用绑定到同一微信开放平台账号。

建议所有后端回调统一使用 `api.fateinsight.site`。上线前需在微信后台再次确认回调域名的填写粒度和当时平台要求。

### 3.3 公众号授权范围

| Scope | 用途 | 交互 | 建议 |
|---|---|---|---|
| `snsapi_base` | 获取公众号侧 `openid` | 通常静默授权 | 首期登录优先 |
| `snsapi_userinfo` | 获取用户基本资料 | 可能需用户确认 | 确实需要微信头像、昵称时再启用 |

系统已有默认用户名和系统头像机制，所以首期可使用 `snsapi_base`，不应为获取头像强制增加授权摩擦。

## 四、统一登录流程

### 4.1 PC/普通浏览器

```text
用户点击“微信登录”
  → 前端请求 /api/auth/wechat/authorize?channel=open
  → 后端生成 state，保存登录上下文
  → 返回微信网站应用授权地址
  → 前端跳转或展示微信二维码
  → 用户扫码确认
  → 微信回调 /api/auth/wechat/callback/open?code=...&state=...
  → 后端校验 state，code 兑换微信身份
  → 查找或创建站内用户
  → 生成一次性 ticket
  → 302 跳转 /login/wechat/callback?ticket=...
  → 前端 POST /api/auth/wechat/exchange
  → 后端返回现有 AuthResponse
  → 前端 saveAuth 并按建档状态跳转
```

### 4.2 微信内置浏览器

```text
用户点击“微信登录”
  → 前端请求 /api/auth/wechat/authorize?channel=official
  → 后端生成 state，保存登录上下文
  → 跳转公众号网页 OAuth
  → 微信回调 /api/auth/wechat/callback/official?code=...&state=...
  → 后端校验 state，code 兑换 openid/unionid
  → 查找或创建站内用户
  → 生成一次性 ticket
  → 跳转前端回调页并兑换 JWT
```

## 五、数据库设计

### 5.1 新增 `user_identities`

不继续将登录渠道字段直接添加到 `users`。建议新增第三方身份表：

| 字段 | 类型建议 | 说明 |
|---|---|---|
| `id` | BIGINT UNSIGNED | 主键 |
| `user_id` | INT/BIGINT UNSIGNED | 站内用户 ID，外键到 `users.id` |
| `provider` | VARCHAR(32) | `wechat_open` 或 `wechat_official` |
| `provider_app_id` | VARCHAR(64) | 该身份所属的微信 AppID |
| `provider_subject` | VARCHAR(128) | 对应应用下的 `openid` |
| `unionid` | VARCHAR(128), NULL | 微信开放平台统一标识，可空 |
| `nickname` | VARCHAR(128), NULL | 微信资料快照 |
| `avatar_url` | VARCHAR(512), NULL | 微信头像快照 |
| `raw_profile` | JSON/TEXT, NULL | 可选，仅保存允许落库的必要资料 |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 更新时间 |
| `last_login_at` | DATETIME, NULL | 该身份最近登录时间 |

索引与约束：

1. 唯一约束：`provider + provider_app_id + provider_subject`。
2. `user_id` 普通索引。
3. `unionid` 普通索引，待确认开放平台绑定和存量数据后，再决定是否增加更强唯一约束。
4. 外键删除策略与 `users` 的现有数据保留政策一致。

### 5.2 微信身份查找顺序

1. 按 `provider + provider_app_id + openid` 查找。
2. 未找到且微信返回 `unionid` 时，按 `unionid` 查找已有身份。
3. 通过 `unionid` 找到用户时，为当前渠道补建一条身份记录。
4. 仍未找到时创建新用户和新身份。
5. 没有 `unionid` 时不根据昵称、头像或其他弱信号自动合并账号。

### 5.3 一次性状态数据

`state` 和登录 `ticket` 优先保存在 Redis，不新建持久数据表。

`state` 记录建议包含：

- 随机 `state` 值的哈希。
- 渠道。
- 站内跳转目标 `redirect`。
- 登录或绑定用途。
- 发起时间。
- 可选的当前用户 ID（只用于绑定流程）。

建议过期时间：

- OAuth `state`：5 分钟。
- 一次性 `ticket`：30–60 秒。
- 两者都必须原子消费，不能重复使用。

## 六、后端实施方案

### 6.1 模块划分

建议新增：

```text
fate_backend/app/
├── models/user_identity.py
├── schemas/wechat_auth.py
├── routers/wechat_auth.py
└── services/
    ├── wechat_oauth.py
    └── user_identities.py
```

不要将新网站 OAuth 逻辑继续堆入现有 `routers/users.py` 和小程序 `services/wechat.py`。

### 6.2 API 设计

#### `GET /api/auth/wechat/authorize`

请求参数：

- `channel=auto|open|official`
- `redirect=/dashboard` 类型的站内路径

行为：

1. 校验 `redirect` 必须以单个 `/` 开头，禁止 `//`、完整 URL 和非预期协议。
2. 确定授权渠道。
3. 生成至少 128 bit 的密码学随机 `state`。
4. 将登录上下文写入 Redis。
5. 返回授权 URL，或由后端直接 302 跳转。

#### `GET /api/auth/wechat/callback/open`

- 处理网站应用扫码登录回调。
- 校验并原子消费 `state`。
- 用 `code` 兑换微信侧 token 和用户标识。
- 解析统一用户。
- 生成一次性登录 `ticket`。
- 302 跳转前端回调页。

#### `GET /api/auth/wechat/callback/official`

- 处理公众号网页授权回调。
- 其余逻辑与 `open` 回调共用同一身份解析服务。

#### `POST /api/auth/wechat/exchange`

请求：

```json
{
  "ticket": "short-lived-one-time-ticket"
}
```

响应复用现有 `AuthResponse`：

```json
{
  "access_token": "jwt",
  "token_type": "bearer",
  "user": {}
}
```

#### 第二阶段账号管理 API

```text
GET    /api/account/identities
POST   /api/account/identities/wechat/authorize
DELETE /api/account/identities/wechat/{identity_id}
```

解绑前必须确认用户仍保留至少一种可用登录方式，不允许用户将自己锁在账号之外。

### 6.3 后端配置

```env
WECHAT_LOGIN_ENABLED=false

# 网站应用扫码登录
WECHAT_OPEN_APP_ID=
WECHAT_OPEN_APP_SECRET=
WECHAT_OPEN_CALLBACK_URL=https://api.fateinsight.site/api/auth/wechat/callback/open

# 微信内公众号网页授权
WECHAT_OA_APP_ID=
WECHAT_OA_APP_SECRET=
WECHAT_OA_CALLBACK_URL=https://api.fateinsight.site/api/auth/wechat/callback/official

# 前端授权结果页
WECHAT_LOGIN_FRONTEND_CALLBACK_URL=https://fateinsight.site/login/wechat/callback
```

建议用 `WECHAT_LOGIN_ENABLED` 控制功能开关。功能关闭时不展示入口，且后端授权接口返回明确的不可用错误。

生产环境启用微信登录时，启动校验应检查对应 AppID、AppSecret 和回调地址；功能关闭时不应因缺少微信配置阻断整个后端启动。

## 七、前端实施方案

### 7.1 登录页

在现有邮箱和手机号登录表单下方增加独立的“微信登录”按钮，无需增加第三个表单 Tab。

交互规则：

- 普通浏览器：进入扫码授权。
- 微信内置浏览器：直接进入公众号 OAuth。
- 保留现有 `redirect` 参数，但只允许站内路径。
- 点击后立即显示加载状态，防止重复发起。
- 用户取消、授权过期和服务不可用时展示不同的可理解文案。

### 7.2 前端回调页

新增：

```text
fate_frontend/app/(auth)/login/wechat/callback/page.tsx
```

页面职责：

1. 从 URL 取得 `ticket` 或标准化错误码。
2. 立即清理地址栏中的 `ticket`，或在兑换后 `replace` 到干净 URL。
3. POST 调用 `/api/auth/wechat/exchange`。
4. 复用 `saveAuth()` 保存 JWT 和用户。
5. 复用 `checkProfileStatus()` 与 `resolvePostAuthRedirect()`。
6. 兑换失败时允许返回登录页重试。

### 7.3 前端认证封装

在 `app/lib/auth.tsx` 增加：

- `startWechatLogin(channel, redirect)`
- `exchangeWechatTicket(ticket)`
- 微信登录标准化错误类型

登录成功后不单独创建一套微信 Token 存储，继续使用现有 `saveAuth()` 和 JWT 请求链路。

## 八、账号创建、绑定与合并规则

### 8.1 首次微信登录

- 微信身份已绑定：登录对应用户。
- 身份未绑定，但通过 `unionid` 找到同一开放平台下的已有身份：绑定当前渠道并登录原用户。
- 无任何可靠匹配：创建 `source=wechat` 的新用户。

新用户不强制填写邮箱或手机号，但可在建档完成后温和引导绑定手机号，便于账号找回。

### 8.2 已登录用户绑定微信

绑定是一个显式操作：

1. 用户已通过邮箱或手机号登录。
2. 在账号设置点击“绑定微信”。
3. 后端将当前用户 ID 写入受保护的 OAuth `state` 上下文。
4. 授权完成后将微信身份绑定到当前用户。

不允许前端直接传入任意 `user_id` 执行绑定。

### 8.3 冲突处理

如果微信身份已属于另一个站内用户：

- 立即拒绝自动绑定。
- 不自动迁移订单、额度、会员期限、命盘或聊天记录。
- 向用户提示“该微信已关联其他账号”。
- 如未来需要账号合并，必须设计独立、可审计、可回滚的合并流程。

严禁根据微信昵称、头像相似度或未验证的信息静默合并账号。

## 九、安全要求

1. AppSecret 只保存在后端密钥管理或生产环境变量中。
2. 不得将 AppSecret、微信 access token、授权 `code`、完整 `state` 或完整 `ticket` 写入日志。
3. `state` 使用密码学安全随机数，短效且单次消费。
4. 登录 `ticket` 短效且单次消费，只保存哈希或高强度随机值。
5. `redirect` 只允许站内路径，防止开放重定向。
6. 微信 API 请求必须设置连接和读取超时。
7. 对微信错误码分类处理，不将底层异常或密钥信息返回前端。
8. 用户状态为 blocked/deleted 时，即使微信授权成功也不签发 JWT。
9. 数据库依赖唯一约束防止并发回调创建重复身份，并正确处理 `IntegrityError`。
10. 登录、绑定、解绑和冲突均写入不含敏感凭据的审计日志。
11. 交换 JWT 的接口设置速率限制，失败不暴露票据是否曾存在。
12. 仅允许 HTTPS 生产回调。

## 十、错误与用户提示

| 场景 | 前端提示 | 是否可重试 |
|---|---|---|
| 用户取消授权 | 已取消微信登录 | 是 |
| `state` 失效 | 登录请求已过期，请重新尝试 | 是 |
| `ticket` 失效/已使用 | 登录凭证已失效，请重新登录 | 是 |
| 微信 API 超时 | 微信服务暂时不可用 | 是 |
| 账号被禁用 | 账号当前不可用 | 否，引导联系客服 |
| 绑定冲突 | 该微信已关联其他账号 | 否，进入账号处理 |
| 功能开关关闭 | 微信登录暂未开放 | 否，提供邮箱/手机登录 |

后端应返回稳定的业务错误码，前端不应通过解析微信原始错误文字决定交互。

## 十一、测试计划

### 11.1 后端单元测试

- 生成授权 URL 时 AppID、回调地址、scope 和 `state` 正确。
- 非法 `redirect` 被拒绝。
- 缺失、过期、渠道不匹配和重放的 `state` 被拒绝。
- 微信成功、拒绝、错误码、非 JSON 响应、超时等情况。
- 按 openid 命中已有身份。
- 按 unionid 识别已有用户并补建渠道身份。
- 首次登录创建用户和身份。
- 并发回调不产生重复用户或身份。
- blocked/deleted 用户无法登录。
- `ticket` 只能成功兑换一次。
- 绑定冲突不会迁移数据。

### 11.2 前端测试

- 普通浏览器选择 `open` 渠道。
- 微信内置浏览器选择 `official` 渠道。
- 按钮加载状态能防止重复点击。
- 回调页成功兑换后调用 `saveAuth()`。
- 已建档和未建档用户的跳转符合现有逻辑。
- 错误状态不留下本地半登录数据。
- 恶意 `redirect` 不能导向站外。

### 11.3 真实环境验收

- Windows/macOS 主流浏览器扫码登录。
- iOS/Android 微信内置浏览器授权。
- 已关注与未关注公众号用户。
- 同一微信用户分别通过公众号授权和网站扫码登录，在 unionid 可用时必须落到同一站内用户。
- 从定价页、会员页和其他受保护页发起登录后，能返回原目标页。
- 用户取消、网络切换、回调超时和重复刷新回调页。

## 十二、监控与埋点

建议记录以下事件，不包含敏感凭据：

- `wechat_login_click`：属性包含渠道和来源页。
- `wechat_oauth_started`。
- `wechat_oauth_callback_success`。
- `wechat_oauth_callback_failed`：仅记录标准化错误类型。
- `wechat_login_exchange_success`。
- `wechat_login_exchange_failed`。
- `wechat_identity_created`。
- `wechat_identity_linked_by_unionid`。
- `wechat_identity_binding_conflict`。

核心观测指标：

1. 点击→开始授权转化率。
2. 开始授权→回调成功率。
3. 回调成功→JWT 兑换成功率。
4. 微信 API 超时率和错误码分布。
5. 新用户创建率。
6. unionid 跨渠道关联率。
7. 账号绑定冲突率。

## 十三、分阶段实施

### 阶段 0：资质和配置确认

- 完成微信开放平台资质和网站应用审核。
- 确认公众号网页授权能力。
- 将网站应用和公众号绑定到同一开放平台账号。
- 配置授权回调域名。
- 确定生产和测试环境配置方式。

该阶段可与技术开发并行，但真实端到端验收依赖审核通过。

### 阶段 1：统一身份基础

- 新增 `user_identities` 模型和数据库迁移。
- 实现身份查找、创建、unionid 关联和并发冲突处理。
- 实现 Redis `state` 和 `ticket` 服务。
- 完成后端单元测试。

### 阶段 2：PC/普通浏览器扫码登录

- 实现 `wechat_open` OAuth 服务。
- 实现 authorize、open callback 和 exchange 接口。
- 增加登录页入口和前端回调页。
- 复用现有 JWT 和登录后建档跳转。
- 通过功能开关在生产环境小流量开启。

### 阶段 3：微信内公众号网页授权

- 实现 `wechat_official` OAuth 服务和 official callback。
- 实现微信内置浏览器识别和自动选择渠道。
- 验证公众号与网站应用的 unionid 打通。
- 完成 iOS 和 Android 微信真机测试。

### 阶段 4：绑定、解绑和运营优化

- 账号设置页增加微信绑定状态。
- 实现绑定、冲突提示和安全解绑。
- 完善审计日志、监控告警和渠道转化埋点。
- 根据真实登录成功率优化交互和文案。

## 十四、预估工期

不包含微信资质认证和平台审核等待时间：

| 工作项 | 预估 |
|---|---:|
| 身份表、迁移和身份服务 | 1–1.5 人日 |
| `state`/`ticket` 和安全基础 | 0.5–1 人日 |
| 网站应用扫码 OAuth 后端 | 1 人日 |
| 公众号网页 OAuth 后端 | 1 人日 |
| 登录页和回调页 | 0.5–1 人日 |
| 自动化测试、真机联调和灰度 | 1–2 人日 |
| 合计 | 约 5–7.5 人日 |

微信开放平台网站应用审核、公众号资质和回调域名配置是主要外部依赖，应尽早开始。

## 十五、上线检查清单

### 微信平台

- [ ] 网站应用已审核通过。
- [ ] 公众号网页授权能力已确认。
- [ ] 网站应用和公众号已绑定到同一开放平台。
- [ ] 授权回调域名已配置。
- [ ] 生产 AppID/AppSecret 已通过密钥管理注入。

### 后端

- [ ] 数据库迁移已在备份和预发环境验证。
- [ ] Redis 可用且 TTL/原子消费符合预期。
- [ ] 生产 CORS 不使用不必要的 `*`。
- [ ] 所有回调只使用 HTTPS。
- [ ] 日志不包含密钥、code、token 或完整票据。
- [ ] 限流、超时和错误映射已启用。
- [ ] blocked/deleted 账号策略已验证。

### 前端

- [ ] 功能开关关闭时不展示微信入口。
- [ ] PC 扫码和微信内授权交互均已真机验收。
- [ ] 登录后回到原目标页。
- [ ] 回调 URL 中的一次性票据被及时清理。
- [ ] 取消、超时、冲突和平台错误文案已验收。

### 业务和监控

- [ ] 新建微信用户能正常建档、下单和使用权益。
- [ ] 同一用户两个微信渠道不会重复建号。
- [ ] 登录漏斗埋点已上报。
- [ ] 微信 API 错误率和回调失败率告警已配置。
- [ ] 已准备邮箱/手机号登录作为降级通道。

## 十六、上线成功标准

同时满足以下条件时，可认定网站微信登录完成上线：

1. PC/普通浏览器可以通过微信扫码登录。
2. 微信内置浏览器可以通过公众号网页授权登录。
3. 两个渠道最终都兑换成现有 JWT，不影响其他 API。
4. unionid 可用时，同一微信用户不会因渠道不同产生两个站内用户。
5. 授权回调不在 URL 中暴露 JWT，`state` 和 `ticket` 均无法重放。
6. 微信登录用户能正常使用命盘、聊天、订单、会员和额度。
7. 微信服务异常时，邮箱和手机号登录仍然可用。

